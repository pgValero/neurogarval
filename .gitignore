# Artifact generated by build.py (the canonical build is run by the
# .github/workflows/build_validation.yml and deploy_page.yml workflows on push to main).
_site/

__pycache__

# Cache of the ruff hook (prek)
.ruff_cache/
